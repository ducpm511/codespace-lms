node_modules/
dist/
build/
.turbo/
coverage/
*.log

# prisma generated client
generated/

# local file storage (PDF chứng chỉ… — LocalStorageAdapter)
uploads/

# pnpm local store (dùng global store; không commit)
.pnpm-store/

# env
.env
.env.*
!.env.example

# claude local
.claude/settings.local.json
.claude/worktrees/

# Piston tải runtime ngôn ngữ vào đây lúc chạy — không commit
ops/piston-packages/*
!ops/piston-packages/.gitkeep
